#ifndef DAW_UTILS_HPP
#define DAW_UTILS_HPP

#include <iostream>

char* copy(const char* str);
//char* readString(std::istream& is, char delimiter);
//void skipWhiteSpaces(std::istream& is);


#endif //DAW_UTILS_HPP
